{
  "name": "tvm-studio",
  "version": "0.2.0",
  "description": "Interactive Time Value of Money teaching site — by Ian Helfrich.",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist .observablehq/cache",
    "build": "observable build",
    "dev": "observable preview",
    "deploy": "observable deploy"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.0",
    "canvas-confetti": "^1.9.3",
    "d3": "^7.9.0",
    "three": "^0.169.0"
  },
  "engines": {
    "node": ">=20"
  }
}
